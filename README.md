# 

```bash
#!/usr/bin/bash
# プログラムのクローン
git clone https://github.com/RiSec/uppun_bakusan.git

# 移動
cd uppun_bakusan

# パッケージのインストール
npm install

# サーバー起動
npm start
```

```bash
# ブランチの作成
git checkout -b lion/web-speech-api

```



```bash
# ステージング状態にする
git add <ファイル名>

# コミットする
git commit -m <メッセージ> 

# プッシュする
git push
```